# Mada Symphony Score: Release v0.42 "Twin Panels" (Engineering Landing)

**Goal**: Land v0.42 "Twin Panels" into the MRQLab Next.js & Python backend codebase. 
Strictly enforce the **Lens Projection Contract**:
- Backend Python kernel is the Single Source of Truth (`ResultGraph`).
- Frontend TypeScript acts strictly as Editor & Renderer with zero physical math duplication.

---

## 🎼 Orchestra Movements (Tacet & Lifecycle Matrix)

| Movement | Section | Mission & Scope | Deliverables | Verification |
|---|---|---|---|---|
| **I. Overture (Contracts)** | Oboe & Cello | 1. Expand backend `presets.py` & `models.py` with expanded clinical scenario metadata (MSK Knee, Angio TOF, Abdomen, Cardiac, Neuro).<br>2. Add `GET /clinical-scenarios` or enrich `list_clinical_recipes` API in `main.py`. | `packages/mrqlab_experiment/` & `services/api/` | `pytest tests/test_clinical_api.py` |
| **II. Variation (Frontend Engine & Lens Refactor)** | Violin I & Viola | 1. Delete fake physical computation in TS (`compare-engine`, `optimize-engine`, `pulse-inspector-data`).<br>2. Implement `LensProjection` architecture in `apps/web/lib/` & `components/workbench/`.<br>3. Build `PhysicsLensView.tsx` (5-channel timeline, EPG $(t, k)$ phase graph, rotating frame $M(t)$, single NEMA test phantom).<br>4. Build `ClinicalLensView.tsx` (Quad MPR slot grid, 2D MIP Ray-Cast scrubber, real tissue contrast weighting). | `apps/web/components/workbench/` | `npm --prefix apps/web test` |
| **III. Tutti (Deterministic Integration)** | Snare Drum | 1. Run zero-token deterministic ladder: vitest (11+ passed), pytest (168+ passed).<br>2. Next.js build verification (`npm run build`). | Clean build & test logs | 0-Token 100% green |
| **IV. Finale (Deployment & Release)** | Trumpet & Harp | 1. Deploy production static build to Cloudflare Pages.<br>2. Git commit & push branch `feature/v0.42-twin-panels`. | Cloudflare URL & PR | Live verification |
