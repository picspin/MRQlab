# v0.1 Dual-Axis Review (A–H + Task 12)

**Date:** 2026-08-17  
**Tree:** `feature/experiment-kernel-docs` @ `bc12018` / `origin/main` @ `7229f1a`  
**Independent agents:** dispatched via `delegate_task` (`code1-gpt-5.5` / litellm-gateway)

**Parent verdict: PASS-WITH-NITS**

Independent Spec/Standards JSON will be appended when those agents finish. This file is the 7.5-style consolidated gate from a live-tree audit + full test run.

## Verification (parent, this turn)

```
.venv/bin/python -m pytest tests/ -q     → 142 passed, 1 warning
cd apps/web && npm test                  → 4 passed
npm run typecheck                        → pass
npm run build                            → /, /editor, /signal-lab
rg class (SpinEcho|TurboSpinEcho|CEST|ASL).*Sequence|class AdvancedSimulator
                                         → no matches
rg Floquet|CEST|MRS|DCE in experiment/web/api
                                         → no matches
rg FastAPI(                               → one app (services/api/mrqlab_api/main.py:46)
```

Task 12 docs commit is **ahead of main** (`bc12018`). H (#12) is on main.

## Axis A — Spec / lock

| AC | Verdict | Note |
|---|---|---|
| Five contract names | PASS | Nit: `PhysicsOperator` not exported in `__all__` |
| Three-layer IR versioned | PASS-WITH-NITS | Compilers exist; `compile_physics_ir` not on `run_experiment` |
| SE/GRE/TSE numerics green | PASS | 142 |
| Capability + reserved fail-closed | PASS | |
| Shared `/simulate` + `/experiments/run` | PASS | both `run_experiment` |
| ResultGraph + provenance | PASS-WITH-NITS | no `kind="engine"` edges |
| Objective v0, no optimizer | PASS | |
| Disturbance reselection explain | PASS | |
| Shared workspace + cursors | PASS-WITH-NITS | no undo/persist; Explore unbound |
| TSE thesis tests | PASS-WITH-NITS | backend real; UI mocked; k-space label empty |
| Anti-scope | PASS | |

No in-scope AC is missing. Parked items stay parked (see retrospective).

## Axis B — Standards / quality

**Security:** none.

**Logic (not FAIL):** live API copies before write; `run_experiment` uses `plan.engine`; products fail closed; `/simulate` wraps same kernel.

**Nits / design leaks**

1. `apps/web/app/signal-lab/page.tsx` mutates the fetched preset (`graph.sequence.params = …`). Client-side only; kernel still copy-on-resolve. Still a teaching-surface smell.
2. `compile_physics_ir` is a sidecar.
3. Linked Lens / Explore are structural, not projections.
4. `echo_train` / `sar` are teaching proxies (`|signal|`, `echoes*(FA/180)²`).
5. Untracked local `.hermes/plans/*-review.md` files (not in commit).

## Overall

**Approve (PASS-WITH-NITS).** Land Task 12 docs. Do not open Task 13. Next work is a named hardening slice if the user wants one.

See `.hermes/plans/2026-08-17-v01-retrospective.md`.
