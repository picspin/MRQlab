# Task 7.5 Dual-Axis Review

**Verdict: PASS-WITH-NITS**

Reviewed `origin/main @ 1ee5218...047ecac` on `feature/experiment-kernel-7.5`.

## Findings

No P0 findings.

### Nit: duplicate requested product IDs are not rejected

**Location:** `packages/mrqlab_experiment/mrqlab_experiment/observations.py:158`

`ReadoutSpec(products=("signal", "signal"))` emits two observations with the same ID. A graph-addressed result normally benefits from unique IDs, but Task 7.5 requires emission exactly in requested order and does not require uniqueness. Per review instruction, this is non-blocking.

## Axis A: Lock / Spec / TDD Fidelity

- `plan_experiment()` compiles the graph, merges disturbance capabilities, preserves preferred-engine fail-closed behavior, and is used by validation and execution.
- `run_experiment()` simulates with `plan.engine`; TSE metadata preference resolves to EPG.
- Kernel and API resolution do not mutate caller graphs. `KernelRun` holds a deep graph snapshot.
- Result graphs emit only requested products in order, compute internal image precursors without exposing them, and only add emitted `derived_from` sources/edges.
- Unknown, snapshot, `echo_train`, and `sar` products fail closed. Snapshot collection remains disabled.
- `objective_score` without an objective fails closed. Reserved objective observations such as `echo_train` now raise `ValueError`, and the HTTP endpoint maps this to 422 rather than leaking `KeyError` as HTTP 500.
- Observation provenance takes representation from `run.plan.representation`.

## Axis B: Quality / Regressions

No blocking regression found in the changed product paths. The implementation is confined to the locked backend/API/test/documentation scope.

## Missing Tests

No lock-required tests are missing. A duplicate-product behavior test would document the current non-unique-ID behavior if the contract is later tightened.

## Out-of-Scope Edits

No prohibited product areas changed: no `packages/physics/**` numerical backend changes, no `apps/web/**`, and no SequenceIR implementation changes. The lock/TDD briefs and Task 7.5 documentation pointer are in scope.

## Verification

- Focused Task 7.5/API review set: `54 passed, 1 warning`.
- Full suite: `138 passed, 1 warning`.
- `git diff --check origin/main...HEAD` reports only trailing whitespace in newly added lock/TDD Markdown files.
